{
  "name": "hbplugin-translate-chat",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p ./",
    "prepack": "rm -rf dist && yarn build",
    "publish": "yarn npm publish --access public"
  },
  "devDependencies": {
    "@skeldjs/hindenburg": "link:../..",
    "@types/node-fetch": "^2.5.11",
    "@types/ws": "^7.4.5",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "node-fetch": "^2.6.1",
    "ws": "^7.5.1"
  }
}
